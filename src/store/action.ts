import {
  changeCity,
  setOffers,
  setAuthStatus,
  setUser,
  logout,
} from './reducer';

export const actions = {
  changeCity,
  setOffers,
  setAuthStatus,
  setUser,
  logout,
};

export { changeCity, setOffers, setAuthStatus, setUser, logout };
